allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}

ext {
    mapboxSdkToken = "sk.eyJ1IjoieWFzaGd1cHRhMjFjc2UiLCJhIjoiY201bmk5ZjVpMGJkMjJtczl6M2ZrcjI5NyJ9.Wv9lJF3oweFZuCzhCdz2rg"
}